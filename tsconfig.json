{
  "compilerOptions": {
    "module": "ES2015",
    "target": "ES2015",
    "strict": true, //所有严格检查的总开关
    "noEmitOnError": true, //有错误不编译
    "outDir": "./"
  },
  // 需要编译的ts文件一个*表示文件匹配**表示忽略文件的深度问题
  "include": ["src/**/*.ts"],
  "exclude": ["abort-promise-demo/**/*"]
}
